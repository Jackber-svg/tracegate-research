interface:
  display_name: "TraceGate Research"
  short_description: "Fail-closed evidence gates for research agent work"
  default_prompt: "Use $tracegate to audit this research project and tell me whether it can safely promote a verified checkpoint."

policy:
  allow_implicit_invocation: false
